# ngrok v3 — ikki tunnel: webapp (3001), api (3000).
# Authtoken ~/.config/ngrok/ngrok.yml da bo'ladi; bitta process ikkala tunnelni ochadi.
version: 3

agent:
  web_addr: 127.0.0.1:4040

endpoints:
  - name: webapp
    upstream:
      url: 3001
  - name: api
    upstream:
      url: 3000
